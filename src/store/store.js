import {writable} from 'svelte/store';

export const thinkerStoreData = writable({});
export const homeStoreData = writable({});
export const contactStoreData = writable({});
export const designerStoreData = writable({});
export const collectionStoreData = writable({});
export const nomadStoreData = writable({});